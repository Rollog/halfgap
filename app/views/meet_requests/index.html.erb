<% content_for :content do %>
	<table class="table">
		<% @received_requests.each do |r| %>
		<tr>
			<td><%= r.message %></td>
			<td><%= link_to "confirm", confirm_meetup_path(r.id), method: :patch, class: "confirm_mu_link" %></td>
			</tr>
		<% end %>
	</table>
<% end %>